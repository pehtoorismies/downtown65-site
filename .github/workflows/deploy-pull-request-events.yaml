name: Deploy PR Events

on:
  workflow_call:
    inputs:
      pr-number:
        required: true
        type: number
    outputs:
      events-url:
        value: ${{ jobs.deploy.outputs.events-url }}

env:
  URL: "https://events-pr-${{ inputs.pr-number }}.downtown65.com"

jobs:
  deploy:
    runs-on: ubuntu-22.04
    environment: development
    outputs:
      events-url: ${{ env.URL }}

    steps:
      - uses: actions/checkout@v6
      - name: Setup Project
        uses: ./.github/actions/setup-project
      - name: Generate wrangler config for PR
        working-directory: apps/events
        env:
          PR_NUMBER: ${{ inputs.pr-number }}
        run: |
          envsubst '$PR_NUMBER' < wrangler.jsonc > wrangler.jsonc.tmp
          mv wrangler.jsonc.tmp wrangler.jsonc

          echo "Wrangler config after replacements:"
          cat wrangler.jsonc

      - name: Deploy Events
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: apps/events
          environment: pull-request
          preCommands: |
            CLOUDFLARE_ENV=pull-request pnpm run build
          secrets: |
            API_KEY
            COOKIE_SESSION_SECRET
        env:
          API_KEY: ${{ secrets.DT65_API_KEY }}
          COOKIE_SESSION_SECRET: ${{ secrets.FRONTEND_COOKIE_SESSION_SECRET }}

      - name: Display Events URL
        run: |
          echo "### 🚀 Events Deployed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Events URL: ${{ env.URL }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🔗 [Open Events](${{ env.URL }})" >> $GITHUB_STEP_SUMMARY